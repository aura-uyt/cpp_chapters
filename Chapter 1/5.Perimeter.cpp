#include <iostream>
using namespace std;

int main(){
    int l,b,perimeter;
    cout<<"Enter the length of Plot: ";
    cin>>l;

    cout<<"Enter the breadth of Plot: ";
    cin>>b;

    perimeter=2*l+2*b;
    cout<<"Perimeter of Plot is: "<<perimeter;
    cout<<"\n";

    return 0;

}